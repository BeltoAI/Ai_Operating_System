<?xml version="1.0" encoding="utf-8"?>
<!-- P0.4: API 31+ rules. Exclude all app data from BOTH cloud backup and device-to-device transfer,
     so the brain and model keys/tokens can't be pulled off via backup or migration. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="database" path="."/>
        <exclude domain="file" path="."/>
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="database" path="."/>
        <exclude domain="file" path="."/>
    </device-transfer>
</data-extraction-rules>
